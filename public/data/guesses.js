///list of guessed items for remove game
// var http = require('http');
// var fs = require ('fs');
// var express = require ("express");//module that allows this to work as a server that finds all dependent files
// var cors = require("cors");
// var app = express();

// var topten = [
// ];

// app.use(function(req, res, next) {
// 	console.log(`${req.method} request for '${req.url}'`);
// 	next();
// });//gives a conclose log for what is requested

// app.use(express.static("./public"));

// app.use(cors());

// app.get("/guesses-api", function(req, res){
// 	res.json(topten);
// });//makes topten list into an api file
